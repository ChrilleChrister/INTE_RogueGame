package se.su.dsv.inte.combatTest;

import org.junit.Test;

import se.su.dsv.inte.*;
import se.su.dsv.inte.combat.Engagement;

import static org.junit.Assert.*;

public class EngagementTest {

    // private final PlayerCharacter DEFAULT_PLAYER = new PlayerCharacter(Race.HOBBIT, "DefaultPlayer");
    // private final EnemyCharacter DEFAULT_ENEMY = new EnemyCharacter(Race.WOLF, "DefaultEnemy");

    @Test
    //TileType ska tas in, ej implementerat i denna branch ännu
    public void testCtrSetsAttributes(PlayerCharacter player, EnemyCharacter enemy){
        // new Engagement(DEFAULT_PLAYER, DEFAULT_ENEMY);
        // assertEquals(1, 1);
    }
}
